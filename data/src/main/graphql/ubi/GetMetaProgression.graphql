query GetMetaProgression {
    viewer {
        id
        level
        levelProgression
        nextLevelXp
        xp

        nextLevelRewards {
             nodes {
                 id
                 iconUrl
                 quantity
                 type
             }
        }
    }
}